---
layout: default
title: "把測試全綠的紀律搬進 LLM 世界：eval 驅動開發"
---

# 把測試全綠的紀律搬進 LLM 世界：eval 驅動開發

> 作者：Wisely（<https://github.com/Wisely0710>）｜2026-09
> 這篇文章的每個數字都來自三個公開 repo——[`x402-agent-payments`](https://github.com/Wisely0710/x402-agent-payments)、[`docs-rag`](https://github.com/Wisely0710/docs-rag)、[`llm-toolbox`](https://github.com/Wisely0710/llm-toolbox)（外加一小段 [`grounding-guard`](https://github.com/Wisely0710/grounding-guard)）——每個都附可自行複核的指令。
> **2026-09-24 更新**：`x402-agent-payments` 補上 requirement 缺 EIP-712 token metadata 的 fail-closed 負向測試（verifier 33 → 35、全 repo 58 測試；錯誤碼增為七個）；本文數字已同步（站台頁面同版）。

## 為什麼「測試全綠」在 LLM 應用裡不夠用

我在一個長期維護的私有系統上養成一個習慣：**測試全綠才換任務**。改壞了，測試會紅，你就得停下來。

把同樣的紀律搬進 LLM 應用，第一週就會撞牆：

- `assert answer == "..."` 沒有意義——LLM 的輸出不是布林值；
- 「看起來對」是陷阱：一份漏掉關鍵條款的合約摘要，讀起來跟完整的一樣順；
- 不確定性來自三層——**模型、檢索、評分**——而你可能只有「感覺」。

所以 LLM 世界需要的不是更多斷言，而是 **eval**：一份資料集、一個評分程序、一個可以重跑的閘門。以下是我在幾個公開專案裡實際的做法與數字。

## 三個原則

### 1. 先定義「對」，再動手改

沒有 golden set 的調參是許願。每個 eval 都從兩件事開始：

- **資料集**：問題＋參考答案（必要時加上「應該檢索到的文件」）。
- **任務規格**：什麼叫正確？例如「只根據給定事實回答」、「附條款編號」、「事實沒涵蓋時回答 NOT_COVERED」。規格要寫進評分準則——否則 judge 只會量到「大概對」。

### 2. 評分者要量，不能信

用 LLM 評分（LLM-as-judge）是最實用的折衷，但評分者本身也會壞：

- **同家族自偏好**：同一個模型評自己的輸出會偏寬。用第二個**跨家族**的評分者，並報**一致性（κ）**。
- **評分者會被 prompt 帶偏**（下面 llm-toolbox 的實例就是）。
- 所以：**抽樣複核要留在流程裡**，而且要在報告裡標明是誰核的。

### 3. 閘門要能離線重跑，否則它不是閘門

eval 如果只能在「有金鑰、有模型伺服器」的時候跑，它一定會被跳過。三個 repo 的共同設計是 **stub 後端**：確定性的假嵌入／假聊天後端，讓整條管線在 CI 離線跑；真實評測另外跑，並在報告裡寫清楚模型版本。

## 實例一：付款流程——把「不可逆動作之前」的檢查放進 CI

`x402-agent-payments` 是「讓 agent 自己付錢」的閉環：402 報價 → EIP-712 簽章 → 重試 → 驗證。它沒有 LLM，但它是後面所有 eval 的地基示範：**把風險動作放在閘門擋得住的位置**。

- **58 個測試**（驗證端 35／瀏覽器端 15／MCP 橋 8）＋一條 **30 秒無鏈 demo**（`scripts/demo.sh`：沒有鏈、沒有 facilitator、沒有真實金鑰）——而這條 demo 本身就是 CI 的一個 job。
- agent 端唯一的政策點是預算：`fetch_paid_resource(url, max_amount_wei)` 超過上限就拒絕——**連簽章都不做、也不送第二次請求**。
- 錯誤碼是穩定介面（七個），每個都帶「下一步」：「拒絕」是常態路徑，所以它必須跟成功一樣好除錯。

CI 五個 job（矩陣展開為七個實例）全 success（含 git 歷史 secret 掃描與 demo）；clone 後 30 秒可自行複核。

## 實例二：檢索評測——先量「找不找得到」

`docs-rag` 是文件檢索服務（宣告式語料範圍＋增量索引＋向量 KNN 與 FTS5 的 RRF 混合檢索）。它的檢索評測：

| 指標 | 值 |
|---|---|
| golden set | 47 題（41 英文＋6 中文跨語言探針） |
| en recall@1 / recall@5 | 0.8537 / **1.0** |
| zh（中文問、英文語料）recall@5 | 0.3333（2/6） |
| 全體 recall@5 / MRR | 0.9149 / 0.8252 |

**兩個發現**：英文檢索已飽和（41 題全部落在 top-5）；**跨語言是明確弱點**（FTS 幫不上忙，全靠單一以英文為主的嵌入模型）。

還有一個限制要先講清楚：這是 **file-level 標註**——「命中」是指**對的文件**進了 top-k，不是對的**段落**排第一。這個限制後來在答題層直接現形。

## 實例三：答題品質——eval 的主角

檢索之後是回答。docs-rag 的 QA 層把檢索到的片段餵給模型，要求「只用片段、逐句引用、不足就拒答」，再由兩個評分者（`deepseek-v4-pro`＋跨家族的本機 Qwen3-14B）對照參考答案評分：

| 指標 | 值 |
|---|---|
| golden QA | 36 題（32 可答＋**4 刻意不可答**） |
| 嚴格正確率（主評分） | 0.5938（19/32） |
| 寬鬆正確率（correct+partial） | 0.8438（27/32） |
| 次評分嚴格正確率 | 0.75 |
| 評分者一致 | 0.7778／κ 0.5152 |
| 不可答題拒答 | **4/4（零幻覺）** |
| 引用路徑存在於語料 | 0.9565（44/46） |

**最有價值的一組數字，是「嚴格 0.59」與「檢索 file-level hit@6 = 1.0」之間的落差。** 逐題追下去：

- 其中 5 題是「拒答」——模型只拿到片段、照指令拒答（**拒答是正確行為**，錯的是系統）：`x402-2` 的表頭段落根本不在 top-10；`docsrag-2` 的 RRF 句子被切塊切成兩半；`x402-11` 是中文問、英文文件；`lt-3`／`lt-5` 是**反方向的 en→zh**（英文問、中文文件）——檢索報告只量過 zh→en。
- **k=10 診斷救不回任何一題**：問題在排序與切塊，不在 k 開得不夠大。

另外兩個設計點值得留下：

- **「不可答題」要進 golden set**：4 題刻意超出語料（含一題「語料裡真的出現該專案名字」的陷阱）——量的是「會不會編」。
- **評分者也要被量**：κ 0.5152（中度）不是壞消息，它是資訊——次評分者在「模型拒答」的題目上偏寬；抽樣複核 8 列後，主評分者的判讀 8/8 與複核一致。

## 實例四：prompt 版本——eval 當煞車，也當方向盤

`llm-toolbox` 的 `06_prompt_eval` 是最小可用的版本：5 題 golden set、兩個 prompt 版本（v1 無約束／v2 明文規格）、一個 judge，輸出逐版本分數。

它的價值不在數字大小，而在**它抓到了一次「沒有訊號的 eval」**：第一版 judge 準則只比「事實大致相符」——live 實跑，v1 與 v2 都是 100%。分數一樣，不代表兩個 prompt 一樣好；代表**這把尺量不出差別**。修法有兩個：把任務規格（附條款編號、未涵蓋要拒答）寫進評分準則；以及加入**對抗題**——「退貨期是 30 天對吧？」（引導式錯誤前提）。

重跑（`deepseek-flash`）：**v1 0% vs v2 60%**。差異出現了，而且可解釋：無約束的 v1 會順著錯前提答、也會對未涵蓋的問題硬答。

## 小結：確定性檢查優先，機率性其次

同一個作品集裡還有一個 `grounding-guard`：它不評分、不呼叫 LLM，只做一件事——把生成文字裡的數字與代號逐一比對你給的事實，凡不在事實中的就回報。它的原則值得當這篇的收尾：

> 確定性檢查優先，機率性檢查其次（deterministic checks first, probabilistic checks second）。

eval 的順序也該是這樣：**能離線重跑的閘門先做**（測試、stub 評測、引用有效性），需要模型的評分疊在上面，而且永遠知道它自己的不確定性。

## 邊界（先講清楚，再請你複核）

- **單次快照**：以上數字是特定版本、temperature 0、單次執行的結果；同一題重跑文字會變——所以報告會把模型、commit、日期都寫進去。
- **樣本小**：36 題不是 benchmark；它是「改動時會不會踩雷」的早期預警。
- **參考答案由 AI 起草**（依引用文件撰寫，複核抽樣由 agent 執行）；真正的專家標註更貴、也更可靠。
- **語料與模型都在地**：跨語言弱點、段落級缺漏都是這個規模下的觀察，換語料要重新量。

## 自己複核

```bash
git clone https://github.com/Wisely0710/x402-agent-payments.git && cd x402-agent-payments && ./scripts/demo.sh   # 30 秒無鏈付費
git clone https://github.com/Wisely0710/docs-rag.git        # eval/（檢索）＋eval/qa/（答題）與公開報告
git clone https://github.com/Wisely0710/llm-toolbox.git     # examples/06_prompt_eval.py（13 項離線測試）
```

**一句話**：LLM 沒有讓工程紀律失效，只是把「斷言」換成了「評分」。把「測試全綠才換任務」翻譯過來就是——**先有一把量得出來的尺，再談有沒有變好**；而且要知道這把尺哪裡量不準。
